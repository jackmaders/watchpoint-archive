/**
 * Provides fundamental mathematical helper functions for shared numerical computations.
 *
 * Implements and exports pure arithmetic helper functions such as `add`, ensuring deterministic
 * operations across shared services and utilities.
 */

export function add(a: number, b: number): number {
	return a + b;
}
