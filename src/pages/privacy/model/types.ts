/**
 * Defines domain types and presentation props for the platform Privacy Statement view.
 *
 * Implements TypeScript interface definitions within `src/pages/privacy/model/types.ts`
 * supporting server-side data loading and client-side page rendering.
 */

export interface PrivacyPageProps {
	registrationEnabled?: boolean;
}
