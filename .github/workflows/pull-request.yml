name: PR Quality Checks

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:

  storybook-a11y:
    name: ♿ Run Accessibility Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 🛒 Checkout Source Code
        uses: actions/checkout@v5

      - name: ⚡ Setup Bun Environment
        uses: ./.github/actions/setup-bun

      - name: 🌐 Setup Playwright Environment
        uses: ./.github/actions/setup-playwright
        with:
          browsers: "chromium"

      - name: 🧭 Check Story Completeness
        run: bun run storybook:check:completeness

      - name: 🏗️ Build Storybook App
        run: bun run storybook:build

      - name: ♿ Run Accessibility Checks
        run: bun run storybook:check:browser

      - name: 📩 Upload Failure Artifacts
        if: failure()
        uses: actions/upload-artifact@v5
        with:
          name: storybook-a11y-artifacts
          path: |
            storybook-static/
            test-results/
            playwright-report/
          retention-days: 14

  e2e-tests:
    name: 🎭 Run E2E Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 🛒 Checkout Source Code
        uses: actions/checkout@v5

      - name: ⚡ Setup Bun Environment
        uses: ./.github/actions/setup-bun

      - name: 🌐 Setup Playwright Environment
        uses: ./.github/actions/setup-playwright
        with:
          browsers: "chromium firefox"

      - name: 🗄️ Apply Local Migrations
        run: bun run db:migrate:local

      - name: 🌱 Seed Local Database
        run: bun run check:seed

      - name: 🧪 Run Browser Tests
        run: bun run test:e2e

      - name: 📩 Upload Failure Report
        if: failure()
        uses: actions/upload-artifact@v5
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

  lint:
    name: ✨ Check Code Quality
    runs-on: ubuntu-latest
    steps:
      - name: 🛒 Checkout Source Code
        uses: actions/checkout@v5

      - name: ⚡ Setup Bun Environment
        uses: ./.github/actions/setup-bun

      - name: 🚨 Verify Drizzle Sync
        run: bun run check:drizzle-sync

      - name: 🚨 Verify Cloudflare Types
        run: bun run check:cf-sync

      - name: 🧭 Verify Route Inventory
        run: bun run check:routes

      - name: 🦺 Check Type Safety
        run: bun run check:types

      - name: 📐 Check FSD Architecture
        run: bun run check:architecture

      - name: ✨ Check Code Formatting
        run: bun run check:all

  perf-budgets:
    name: ⚡ Check Performance Budgets
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
      SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
      SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
      SENTRY_PROJECT: ${{ secrets.SENTRY_PROJECT }}
      VITE_SENTRY_DSN: ${{ secrets.VITE_SENTRY_DSN }}

    steps:
      - name: 🛒 Checkout Source Code
        uses: actions/checkout@v5

      - name: ⚡ Setup Bun Environment
        uses: ./.github/actions/setup-bun

      - name: 🌐 Setup Playwright Environment
        uses: ./.github/actions/setup-playwright
        with:
          browsers: "chromium"

      - name: 🗄️ Apply Local Migrations
        run: bun run db:migrate:local

      - name: 🌱 Seed Local Database
        run: bun run check:seed

      - name: 🏗️ Build Web Application
        run: bun run build

      - name: 🚀 Validate Worker Deploy
        run: bun run deploy --dry-run

      - name: 🛈 Check Bundle Size
        run: bun run check:bundle-size

      - name: 🚀 Start Preview Server
        run: |
          bun run build-preview &
          for i in $(seq 1 30); do
            if curl -s http://127.0.0.1:3000/ > /dev/null; then
              echo "Preview server is ready on port 3000"
              break
            fi
            sleep 1
          done

      - name: ⏱️ Run Performance Audit
        env:
          BASE_URL: "http://127.0.0.1:3000"
        run: bun run check:perf-budgets

  unit-tests:
    name: 🧪 Run Unit Tests (${{ matrix.shard }}/${{ strategy.job-total }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4]
    steps:
      - name: 🛒 Checkout Source Code
        uses: actions/checkout@v5

      - name: ⚡ Setup Bun Environment
        uses: ./.github/actions/setup-bun

      - name: 🧪 Run Test Shard
        run: bun run test:coverage:shard --shard=${{ matrix.shard }}/${{ strategy.job-total }}

      - name: 📩 Upload Shard Coverage Report Blob
        uses: actions/upload-artifact@v5
        with:
          name: vitest-reports-${{ matrix.shard }}
          path: .vitest-reports/
          retention-days: 1
          include-hidden-files: true

  unit-tests-coverage:
    name: 📊 Verify Test Coverage
    needs: unit-tests
    runs-on: ubuntu-latest
    steps:
      - name: 🛒 Checkout Source Code
        uses: actions/checkout@v5

      - name: ⚡ Setup Bun Environment
        uses: ./.github/actions/setup-bun

      - name: 📥 Download Shard Coverage Report Blobs
        uses: actions/download-artifact@v5
        with:
          pattern: vitest-reports-*
          merge-multiple: true
          path: .vitest-reports/

      - name: 📊 Merge Reports & Verify Coverage Thresholds
        run: bun run test:coverage:merge

